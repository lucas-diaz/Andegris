// == Import : npm
import React from 'react';
import PropTypes from 'prop-types';

// == Import : local
import './styles.css';

// == Composant
const Home = ({ }) => (
  <div className="home">
    <h2>home</h2>
  </div>
);

Home.propTypes = {

};

Home.defaultProps = {

};
// == Export
export default Home;
